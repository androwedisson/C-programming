#include<stdio.h>
int main(){
    int n,temp;
    printf("enter the elements:");
    scanf("%d",&n);
    int arr[n];
    printf("enter the elements to store:");
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int max=arr[0];
    for(int i=0;i<n-1;i++){
        for(int j=0;j<n-i-1;j++){
            if(arr[j]<arr[j+1]){
                temp=arr[j];
                arr[j]=arr[j+1];
                arr[j+1]=temp;
            }
        }
    }
  printf("%d",arr[1]);
    return 0;
}
