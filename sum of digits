#include<stdio.h>
int main(){
    int sum=0,num,rem;
    printf("enter a number:");
    scanf("%d",&num);
    while(num!=0){
    rem=num%10;
    sum=sum+rem;
    num=num/10;}
    printf("sum of the digits is:%d",sum);
    return 0;
}
